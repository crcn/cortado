class Logger

  ###
  ###

  constructor: (name) ->
    label = "(#{name})"
    while label.length < 12
      label += " "

    @_label = label

  ###
  ###

  log: () ->


exports.load = () ->
  
  create = (name) ->
  
  get: (name) ->



