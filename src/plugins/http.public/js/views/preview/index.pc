
<!--a href="#" data-bind="{{ onClick: showPreview = !showPreview }}">
  {{ showPreview ? "hide preview" : "show preview" }}
</a-->

<div data-bind="{{ show: true }}">

  {{ 
    iframe: {
      class: "test-preview",
      src: location,
      onLoad: _onIFrameLoad
    }
  }}
</div>
